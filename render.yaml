services:
  - type: web
    name: zakaz-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: BOT_TOKEN
        value: ${BOT_TOKEN}
      - key: SECRET_KEY
        generateValue: true
    plan: free
